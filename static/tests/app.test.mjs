import test from'node:test';import assert from'node:assert/strict';import{detectFormat,chunkText,wavBlob}from'../app.js';test('detects golden path formats',()=>{assert.equal(detectFormat({name:'book.epub'}),'epub');assert.equal(detectFormat({name:'book.pdf'}),'pdf');assert.equal(detectFormat({name:'book.txt'}),'text')});test('chunks text without loss',()=>{const text='one two three four';assert.deepEqual(chunkText(text,7),['one two','three','four'])});test('writes a wav blob',()=>assert.equal(wavBlob(new Float32Array([0,-.5,.5])).size,50));
